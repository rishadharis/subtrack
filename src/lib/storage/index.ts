/**
 * Storage barrel — public entry point for the file storage layer.
 *
 * Import from here:
 *   import { loadFromFile, saveToFile, exportBackup, importBackup, getAppData, getIsDirty, ... } from '$lib/storage';
 */

export * from './fileStorage';
